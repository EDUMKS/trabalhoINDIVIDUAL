import { StyleSheet } from "react-native";

export const estilobotao = StyleSheet.create({
  button: {
    backgroundColor: '#000',
    width: '90%',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
    height: 50,
  },

  textButton: {
    fontSize: 35,
    color: "red",
    fontWeight: '900'
  }
});